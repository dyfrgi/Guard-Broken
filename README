An example setup of Guard wherein it fails to reread the Guardfile.

To run:
  bundle install
  start guard in a separate terminal
  run ./test.sh
  observe that guard doesn't display "This won't be shown."
